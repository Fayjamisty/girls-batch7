package variableTypePackage;

public class VariablesTypes {
     /* There are three types of variables;
     i. Instance/global variables -- The variables declares inside the class are called Instance variable
    ii. Local variables -- The variables declare inside the methods, are called local variables
    iii. static variables -- Static variables belongs to the class.This is call class level variables.Static
    variablee we create when we see the variable/ variables are common to all objects.
      */

    byte age = 100; // we both declare and initialize/define the variable
    // sort checkingACCount; // we declare the variable


    public void study() {   // Function -- Method can hold variables, statements

        System.out.println(age);
        //checkingAccount = 32767;   //initialize/define the variable
        System.out.println("checkingAccount");
        boolean snooringInTheClass;
        snooringInTheClass = true;


    }


    }